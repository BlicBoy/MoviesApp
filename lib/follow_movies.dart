import 'package:flutter/material.dart';

import 'package:movies/list_favorite_movies.dart';

class FollowMovies extends StatelessWidget {
  const FollowMovies({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListFavoriteMovies();
  }
}
